SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/server)

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/server/src)

SET(REDISPP_ROOT_DIR src/redispp)
ADD_LIBRARY(redispp STATIC ${REDISPP_ROOT_DIR}/redispp.cpp)

ADD_EXECUTABLE(server server.cpp )
TARGET_LINK_LIBRARIES(server redispp)

